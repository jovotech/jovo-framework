import { app } from './app';

export const handler = async (event: any, context: any, callback: () => void) => {
  // await app.bootstrap();
  // const response = await app.handle(event);
  // return response;
};
