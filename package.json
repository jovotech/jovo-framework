{
  "name": "jovo-framework-root",
  "private": true,
  "version": "4.0.0",
  "scripts": {
    "postinstall": "npm run bootstrap",
    "setup:dev": "npm run bootstrap && npm run build",
    "bootstrap": "lerna bootstrap --hoist --no-ci",
    "build": "lerna exec npm run build --ignore jovo-examples-* --ignore jovo-client-*",
    "prettier": "lerna exec npm run prettier --ignore jovo-examples-*",
    "eslint": "lerna exec npm run eslint --ignore jovo-examples-*"
  },
  "author": "jovotech",
  "license": "Apache-2.0",
  "devDependencies": {
    "@types/jest": "^26.0.19",
    "@typescript-eslint/eslint-plugin": "^4.12.0",
    "@typescript-eslint/parser": "^4.12.0",
    "eslint": "^7.17.0",
    "eslint-config-prettier": "^7.1.0",
    "eslint-plugin-prettier": "^3.3.1",
    "lerna": "^3.22.1",
    "jest": "^26.6.3",
    "prettier": "^2.2.1",
    "rimraf": "^3.0.2",
    "ts-jest": "^26.4.4",
    "typescript": "^4.1.3"
  }
}
