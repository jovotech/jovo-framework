{
	"name": "jovo-framework-root",
	"private": true,
	"version": "4.0.0",
	"description": "",
	"main": "index.js",
	"scripts": {
		"setup:dev": "npm run bootstrap && npm run build",
		"bootstrap": "lerna bootstrap --hoist --no-ci",
		"build": "lerna exec npm run build --ignore jovo-examples-* --ignore jovo-client-*"
	},
	"author": "jovotech",
	"license": "Apache-2.0"
}